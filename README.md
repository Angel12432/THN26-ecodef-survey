# Defense Survey Web

This repository acts as a base for hosting our web based survey. It contains standard web assets (index.html, as well as css, js and json scripts and assets).

With a simple `git clone` you can access this survey locally.
